@{
    ViewData["Title"] = "Home Page";
}

<!DOCTYPE html>
<html lang="bs"><head><meta http-equiv="Content-Type" content="text/html; charset=UTF-8">

<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>KvizApp – Student Dashboard</title>
<link href="./02-dashboard-student_files/css2" rel="stylesheet">
<style>
  :root {
    --bg: #080C14; --surface: #0F1620; --card: #141D2B;
    --border: #1E2D42; --accent: #00E5FF; --amber: #FFB300;
    --text: #E8EDF5; --muted: #5A7090; --success: #00E676; --danger: #FF5252;
  }
  * { margin:0; padding:0; box-sizing:border-box; }
  body { background:var(--bg); color:var(--text); font-family:'Nunito Sans',sans-serif; min-height:100vh; }
  .layout { display:flex; min-height:100vh; }

  /* Sidebar */
  .sidebar {
    width:220px; background:var(--surface); border-right:1px solid var(--border);
    padding:24px 0; display:flex; flex-direction:column; flex-shrink:0;
  }
  .logo {
    font-family:'Rajdhani',sans-serif; font-size:1.6rem; font-weight:700;
    letter-spacing:2px; color:var(--text); padding:0 24px 24px;
    border-bottom:1px solid var(--border);
  }
  .logo span { color:var(--accent); }
  .nav { padding:20px 12px; flex:1; }
  .nav-item {
    display:flex; align-items:center; gap:12px;
    padding:10px 12px; border-radius:8px; cursor:pointer;
    font-size:0.9rem; color:var(--muted); margin-bottom:4px;
    transition:all 0.2s; text-decoration:none;
  }
  .nav-item:hover, .nav-item.active {
    background:rgba(0,229,255,0.08); color:var(--accent);
  }
  .nav-item .ico { font-size:1.1rem; width:20px; text-align:center; }
  .user-card {
    margin:12px; padding:12px; background:var(--card);
    border:1px solid var(--border); border-radius:10px;
  }
  .user-name { font-weight:600; font-size:0.9rem; }
  .user-role {
    font-size:0.72rem; letter-spacing:2px; color:var(--accent);
    text-transform:uppercase; margin-top:2px;
  }

  /* Main */
  .main { flex:1; padding:32px; overflow-y:auto; }
  .topbar {
    display:flex; justify-content:space-between; align-items:center;
    margin-bottom:32px;
  }
  .page-title {
    font-family:'Rajdhani',sans-serif; font-size:1.8rem;
    font-weight:700; letter-spacing:1px;
  }
  .page-title span { color:var(--accent); }
  .date-badge {
    font-size:0.78rem; letter-spacing:2px; color:var(--muted);
    text-transform:uppercase;
  }

  /* Stats row */
  .stats { display:grid; grid-template-columns:repeat(4,1fr); gap:16px; margin-bottom:28px; }
  .stat-card {
    background:var(--card); border:1px solid var(--border);
    border-radius:12px; padding:20px;
    animation:fadeUp 0.5s ease both;
  }
  .stat-card:nth-child(2) { animation-delay:0.1s; }
  .stat-card:nth-child(3) { animation-delay:0.2s; }
  .stat-card:nth-child(4) { animation-delay:0.3s; }
  .stat-label { font-size:0.72rem; letter-spacing:2px; color:var(--muted); text-transform:uppercase; margin-bottom:10px; }
  .stat-val { font-family:'Rajdhani',sans-serif; font-size:2.2rem; font-weight:700; }
  .stat-val.accent { color:var(--accent); }
  .stat-val.amber { color:var(--amber); }
  .stat-val.success { color:var(--success); }
  .stat-sub { font-size:0.78rem; color:var(--muted); margin-top:4px; }

  /* Two col */
  .row2 { display:grid; grid-template-columns:1fr 1fr; gap:20px; margin-bottom:20px; }

  .section-card {
    background:var(--card); border:1px solid var(--border);
    border-radius:12px; padding:24px;
    animation:fadeUp 0.5s ease 0.2s both;
  }
  .section-title {
    font-family:'Rajdhani',sans-serif; font-size:1rem;
    font-weight:600; letter-spacing:2px; text-transform:uppercase;
    color:var(--muted); margin-bottom:18px;
    display:flex; align-items:center; gap:8px;
  }
  .section-title::before { content:''; width:3px; height:14px; background:var(--accent); border-radius:2px; }

  /* Quick actions */
  .action-btn {
    display:flex; align-items:center; gap:14px;
    padding:14px; background:var(--surface);
    border:1px solid var(--border); border-radius:10px;
    cursor:pointer; transition:all 0.2s; margin-bottom:10px;
    text-decoration:none; color:var(--text);
  }
  .action-btn:hover { border-color:var(--accent); background:rgba(0,229,255,0.05); }
  .action-icon {
    width:40px; height:40px; border-radius:8px;
    background:rgba(0,229,255,0.1); display:flex;
    align-items:center; justify-content:center; font-size:1.2rem; flex-shrink:0;
  }
  .action-icon.amber { background:rgba(255,179,0,0.1); }
  .action-text { font-size:0.9rem; font-weight:600; }
  .action-sub { font-size:0.78rem; color:var(--muted); margin-top:2px; }

  /* Recent activity */
  .activity-item {
    display:flex; align-items:center; gap:12px;
    padding:10px 0; border-bottom:1px solid var(--border);
  }
  .activity-item:last-child { border-bottom:none; }
  .activity-dot { width:8px; height:8px; border-radius:50%; background:var(--accent); flex-shrink:0; }
  .activity-dot.amber { background:var(--amber); }
  .activity-dot.muted { background:var(--border); }
  .activity-info { flex:1; }
  .activity-name { font-size:0.88rem; font-weight:600; }
  .activity-time { font-size:0.75rem; color:var(--muted); margin-top:2px; }
  .activity-score {
    font-family:'Rajdhani',sans-serif; font-weight:700;
    color:var(--accent); font-size:1rem;
  }

  /* Subjects */
  .subject-list { display:flex; flex-direction:column; gap:10px; }
  .subject-item {
    padding:12px 14px; background:var(--surface);
    border:1px solid var(--border); border-radius:8px;
    display:flex; justify-content:space-between; align-items:center;
    cursor:pointer; transition:all 0.2s;
  }
  .subject-item:hover { border-color:var(--accent); }
  .subject-name { font-size:0.9rem; font-weight:600; }
  .subject-count { font-size:0.75rem; color:var(--muted); }
  .progress-bar {
    height:3px; background:var(--border); border-radius:3px;
    margin-top:6px; overflow:hidden;
  }
  .progress-fill {
    height:100%; background:var(--accent); border-radius:3px;
    transition:width 1s ease;
  }

  @@keyframes fadeUp {
    from { opacity:0; transform:translateY(16px); }
    to { opacity:1; transform:translateY(0); }
  }
</style>
</head>
<body>
<div class="layout">
  <div class="sidebar">
    <div class="logo">KVIZ<span>APP</span></div>
    <nav class="nav">
      <a class="nav-item active" href="file:///C:/Users/User/Desktop/fax/IV%20semestar/OOAD/files/02-dashboard-student.html#"><span class="ico">⊞</span> Početna</a>
      <a class="nav-item" href="file:///C:/Users/User/Desktop/fax/IV%20semestar/OOAD/files/02-dashboard-student.html#"><span class="ico">📚</span> Priprema</a>
      <a class="nav-item" href="file:///C:/Users/User/Desktop/fax/IV%20semestar/OOAD/files/02-dashboard-student.html#"><span class="ico">⏱</span> Simulacija</a>
      <a class="nav-item" href="file:///C:/Users/User/Desktop/fax/IV%20semestar/OOAD/files/02-dashboard-student.html#"><span class="ico">🏆</span> Leaderboard</a>
      <a class="nav-item" href="file:///C:/Users/User/Desktop/fax/IV%20semestar/OOAD/files/02-dashboard-student.html#"><span class="ico">💬</span> Feedback</a>
      <a class="nav-item" href="file:///C:/Users/User/Desktop/fax/IV%20semestar/OOAD/files/02-dashboard-student.html#"><span class="ico">⚙</span> Postavke</a>
    </nav>
    <div class="user-card">
      <div class="user-name">Emin Begić</div>
      <div class="user-role">Doktor</div>
    </div>
  </div>

  <main class="main">
    <div class="topbar">
      <div class="page-title">Dobrodošao, <span>Emin</span> 👋</div>
      <div class="date-badge">Subota · 11. April 2026</div>
    </div>

    <div class="stats">
      <div class="stat-card">
        <div class="stat-label">Kvizovi završeni</div>
        <div class="stat-val accent">24</div>
        <div class="stat-sub">+3 ove sedmice</div>
      </div>
      <div class="stat-card">
        <div class="stat-label">Prosječan rezultat</div>
        <div class="stat-val amber">78%</div>
        <div class="stat-sub">Zadnji: 82%</div>
      </div>
      <div class="stat-card">
        <div class="stat-label">Rang na listi</div>
        <div class="stat-val success">#7</div>
        <div class="stat-sub">od 143 studenta</div>
      </div>
      <div class="stat-card">
        <div class="stat-label">Predmeti</div>
        <div class="stat-val">5</div>
        <div class="stat-sub">aktivnih predmeta</div>
      </div>
    </div>

    <div class="row2">
      <div class="section-card">
        <div class="section-title">Brze akcije</div>
        <a class="action-btn" href="file:///C:/Users/User/Desktop/fax/IV%20semestar/OOAD/files/02-dashboard-student.html#">
          <div class="action-icon">📖</div>
          <div>
            <div class="action-text">Priprema za kviz</div>
            <div class="action-sub">Vježbaj bez vremenskog ograničenja</div>
          </div>
        </a>
        <a class="action-btn" href="file:///C:/Users/User/Desktop/fax/IV%20semestar/OOAD/files/02-dashboard-student.html#">
          <div class="action-icon amber">⏱</div>
          <div>
            <div class="action-text">Simulacija kviza</div>
            <div class="action-sub">Realni uslovi testiranja</div>
          </div>
        </a>
        <a class="action-btn" href="file:///C:/Users/User/Desktop/fax/IV%20semestar/OOAD/files/02-dashboard-student.html#">
          <div class="action-icon">💬</div>
          <div>
            <div class="action-text">Pošalji feedback</div>
            <div class="action-sub">Prijedlog pitanja ili prijava greške</div>
          </div>
        </a>
      </div>

      <div class="section-card">
        <div class="section-title">Nedavna aktivnost</div>
        <div class="activity-item">
          <div class="activity-dot"></div>
          <div class="activity-info">
            <div class="activity-name">Simulacija – OOAD</div>
            <div class="activity-time">Danas, 10:34</div>
          </div>
          <div class="activity-score">82%</div>
        </div>
        <div class="activity-item">
          <div class="activity-dot amber"></div>
          <div class="activity-info">
            <div class="activity-name">Priprema – Matematika</div>
            <div class="activity-time">Jučer, 19:12</div>
          </div>
          <div class="activity-score" style="color:var(--amber)">45 pit.</div>
        </div>
        <div class="activity-item">
          <div class="activity-dot"></div>
          <div class="activity-info">
            <div class="activity-name">Simulacija – OS</div>
            <div class="activity-time">09. Apr, 14:55</div>
          </div>
          <div class="activity-score">74%</div>
        </div>
        <div class="activity-item">
          <div class="activity-dot muted"></div>
          <div class="activity-info">
            <div class="activity-name">Feedback poslan</div>
            <div class="activity-time">08. Apr, 09:20</div>
          </div>
          <div class="activity-score" style="color:var(--muted); font-size:0.8rem">Prijedlog</div>
        </div>
      </div>
    </div>

    <div class="section-card">
      <div class="section-title">Moji predmeti</div>
      <div class="subject-list">
        <div class="subject-item">
          <div>
            <div class="subject-name">Objektno orijentisana analiza i dizajn</div>
            <div class="progress-bar"><div class="progress-fill" style="width:70%"></div></div>
          </div>
          <div class="subject-count">142 pitanja</div>
        </div>
        <div class="subject-item">
          <div>
            <div class="subject-name">Operativni sistemi</div>
            <div class="progress-bar"><div class="progress-fill" style="width:55%; background:var(--amber)"></div></div>
          </div>
          <div class="subject-count">98 pitanja</div>
        </div>
        <div class="subject-item">
          <div>
            <div class="subject-name">Algoritmi i strukture podataka</div>
            <div class="progress-bar"><div class="progress-fill" style="width:40%"></div></div>
          </div>
          <div class="subject-count">210 pitanja</div>
        </div>
      </div>
    </div>
  </main>
</div>


</body></html>
