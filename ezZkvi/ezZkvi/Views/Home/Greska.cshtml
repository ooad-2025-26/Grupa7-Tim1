@{
    ViewData["Title"] = "Stranica nije pronađena";
    var kod = ViewBag.Kod as int?;
    var jeNotFound = kod == 404;
    var naslov = jeNotFound ? "404 — Stranica ne postoji" : (kod.HasValue ? $"Greška {kod}" : "Došlo je do greške");
    var poruka = jeNotFound
        ? "Stranica koju tražiš ne postoji ili joj se ne može pristupiti upisivanjem adrese."
        : "Nešto je pošlo po zlu. Pokušaj ponovo ili se vrati na početnu.";
}

<div style="min-height:80vh;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;padding:40px 20px;font-family:'Nunito Sans',sans-serif;color:#E8EDF5;">
    <div style="font-family:'Rajdhani',sans-serif;font-size:3rem;font-weight:700;color:#00E5FF;letter-spacing:1px;margin-bottom:10px;">
        @naslov
    </div>
    <p style="color:#5A7090;font-size:1rem;max-width:480px;line-height:1.6;margin-bottom:28px;">
        @poruka
    </p>
    <a asp-controller="Home" asp-action="Index"
       style="display:inline-block;padding:12px 28px;background:#00E5FF;color:#080C14;border-radius:10px;text-decoration:none;font-family:'Rajdhani',sans-serif;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;">
        ← Na početnu
    </a>
</div>
